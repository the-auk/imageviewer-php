README
Author: Tanmay Siwach

Run composer to install monolog and other dependencies in vendor folder. 
Run tilemaker first with two arguments, the first being the image and the second being the directory name.
then run the index.php file using localhost.
